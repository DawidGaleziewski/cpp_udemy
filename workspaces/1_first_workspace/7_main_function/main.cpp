#include <stdio.h>

// one of the files must be a main function which, which is a staring point of the program.
int main(int argc, char **argv) //main can eaither have no params or argc and char params. First argument  is the argument count and the second is the actual arguments
{
	return 0; // we returned value is 0 all went ok. If it is not 0 something went wrong
}
