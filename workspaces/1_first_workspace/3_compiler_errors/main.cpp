#include <stdio.h>

int main(int argc, char **argv)
{
	// printf("hello world\n") // throws syntax error as there is no ";"
    // std::coutt << "Hello world" // Throws a syntax eroor that coutt is not a member of namespace std
	// return; // weeoe as we do not return a value and we declared main as a function that will return one
    return 0;
}
